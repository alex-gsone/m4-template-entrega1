// Importando as interfaces:
import { Product, HandleProducts} from './interfaces';


class ProductList implements HandleProducts{

    private ProductList: Product[] = [];
    public id: number = 1;

    createProduct(data: { name: string; price: number}): Product{
        
        return 
    }
        
    }

}

